# Area IN — infra/

> [← README](README.md)
>
> **Generated by plan-init.** Update the stack details, doc references, and rules to match your project.

**What This Area Is**: Infrastructure as code — provisions Cloud Run, Cloud SQL, Cloud Storage, Secret Manager, Artifact Registry, and IAM on Google Cloud.

---

| Item | Value |
|------|-------|
| Area code | `IN` |
| Directory | `infra/` |
| Stack | Terraform + GitHub Actions |
| Key doc references | `docs/04-architecture/` (deployment topology), `docs/07-hackathon/` (demo environment) |

**Key Checks**: [`[CHECK-AGNOSTIC-BOUNDARY]`](../04-SUB-WORKFLOWS/CHECK-AGNOSTIC-BOUNDARY.md)

---

## Done Criteria

- [ ] Output placed in the correct directory under `infra/`
- [ ] Follows the naming and structure conventions of `infra/`
- [ ] No contradictions introduced with existing documentation
- [ ] TRACEABILITY.md updated with any new terms introduced

---

## Key Rules

- Environments live under `terraform/environments/`; `demo` is the primary hackathon target.
- All cloud resources are provisioned via Terraform — no manual console changes.
- Secrets go in Secret Manager; the Gemini API key is server-side only and never reaches the frontend.

---

> [← README](README.md)
