spring:
  ai:
    vertex:
      ai:
        gemini:
          project-id: ${GOOGLE_CLOUD_PROJECT}
          location: ${VERTEX_AI_LOCATION:us-central1}
          chat:
            options:
              model: ${AI_MODEL_NAME}
