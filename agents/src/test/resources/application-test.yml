spring:
  ai:
    google:
      genai:
        api-key: test-key
        chat:
          options:
            model: test-model
  autoconfigure:
    exclude:
      - org.springframework.ai.model.google.genai.autoconfigure.chat.GoogleGenAiChatAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiEmbeddingConnectionAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiTextEmbeddingAutoConfiguration
      # spring-ai-starter-model-openai (added for Groq, which is OpenAI-compatible) brings in six
      # autoconfiguration classes, all requiring an API key — excluded here for the same reason
      # the Google GenAI ones above are: keep GradeOpsAgentsApplicationTest#contextLoads hermetic
      # under -Pbeta, where the starter is present but no real key is configured for `test`.
      - org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration

app:
  agents:
    gemini:
      # No ChatClient.Builder bean exists here (autoconfiguration excluded above), so the
      # assessment pipeline's beans must stay off too — see AssessmentConfig/AssessmentController.
      enabled: false
