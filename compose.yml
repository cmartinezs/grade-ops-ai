services:

  # ── PostgreSQL ─────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: gradeops
      POSTGRES_USER: gradeops
      POSTGRES_PASSWORD: gradeops
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gradeops -d gradeops"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ── API (Spring Boot) ──────────────────────────────────────────────────────
  api:
    build: ./api
    environment:
      DATABASE_URL: jdbc:postgresql://db:5432/gradeops
      DATABASE_USER: gradeops
      DATABASE_PASSWORD: gradeops
      INTERNAL_API_SECRET: ${INTERNAL_API_SECRET:-dev-secret-change-me}
      CORS_ALLOWED_ORIGINS: http://localhost:3000
      FIREBASE_CREDENTIALS_PATH: /run/secrets/firebase-admin-key.json
      AGENTS_BASE_URL: http://agents:8081
    volumes:
      - ${FIREBASE_ADMIN_KEY_PATH:?Set FIREBASE_ADMIN_KEY_PATH in .env}:/run/secrets/firebase-admin-key.json:ro
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
      agents:
        condition: service_started

  # ── Agents (Spring Boot / Spring AI) ────────────────────────────────────────
  agents:
    build: ./agents
    environment:
      SPRING_PROFILES_ACTIVE: beta
      GRADEOPS_GROQ_API_KEY: ${GRADEOPS_GROQ_API_KEY:?Set GRADEOPS_GROQ_API_KEY in .env}
      GRADEOPS_GROQ_MODEL: ${GRADEOPS_GROQ_MODEL:-llama-3.3-70b-versatile}
      GRADEOPS_GEMINI_API_KEY: ${GRADEOPS_GEMINI_API_KEY:-not-used-locally}
      GRADEOPS_GEMINI_MODEL: ${GRADEOPS_GEMINI_MODEL:-gemini-2.0-flash}
      INTERNAL_API_SECRET: ${INTERNAL_API_SECRET:-dev-secret-change-me}
    ports:
      - "8081:8081"

  # ── Web (Next.js) ──────────────────────────────────────────────────────────
  web:
    build:
      context: ./web
      args:
        NEXT_PUBLIC_FIREBASE_API_KEY: ${NEXT_PUBLIC_FIREBASE_API_KEY:?Set NEXT_PUBLIC_FIREBASE_API_KEY in .env}
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN:?Set NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN in .env}
        NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${NEXT_PUBLIC_FIREBASE_PROJECT_ID:?Set NEXT_PUBLIC_FIREBASE_PROJECT_ID in .env}
        NEXT_PUBLIC_FIREBASE_APP_ID: ${NEXT_PUBLIC_FIREBASE_APP_ID:?Set NEXT_PUBLIC_FIREBASE_APP_ID in .env}
        NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID:?Set NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID in .env}
        NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET:?Set NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET in .env}
        NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: ${NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID:-}
    environment:
      API_BASE_URL: http://api:8080
    ports:
      - "3000:3000"
    depends_on:
      - api

volumes:
  postgres_data:
