Circular avatar for teachers and students. Renders an image when `src` is set, otherwise auto-generated initials with a deterministic tone.

```jsx
<Avatar name="María Fuentes" size="sm" />
<Avatar name="Diego Soto" src={photoUrl} />
```

Sizes: xs / sm / md / lg.
