Checkbox for multi-answer keys, settings toggles in lists, and bulk table selection. Supports an optional `label` and `description`.

```jsx
<Checkbox label="Alternativa C" defaultChecked />
<Checkbox label="Permitir respuestas fuera de tiempo" description="Se marcarán como atrasadas" />
```
