On/off toggle for assessment settings — late submissions, late penalties, share globally, etc. Changing a switch that mutates data should still be confirmed per the GradeOps confirm-before-change rule.

```jsx
<Switch label="Permitir entregas atrasadas" defaultChecked />
<Switch label="Compartir globalmente" />
```
