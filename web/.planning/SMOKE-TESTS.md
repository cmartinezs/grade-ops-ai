# Smoke Test Plan

> Generated by `/plan-smoke-config` or created manually.
> This file describes how to validate the project locally before human code review.

---

## Stack Summary

> *What stack is this project using, and what must be started before smoke checks run?*

- **Primary stack:** [fill in]
- **Supporting services:** [fill in]
- **Start command:** [fill in]
- **Required environment variables:** [fill in]

---

## Execution Order

1. Start every supporting service or fixture required by the stack.
2. Build or compile the project with the project's normal local command.
3. Start the application or worker process using the project-local configuration.
4. Run stack-specific smoke checks that prove the changed surface boots and responds correctly.
5. Collect evidence for human developer review.

---

## Smoke Checks

| # | Check | How to validate |
|---|-------|----------------|
| 1 | Supporting services are ready | [fill in the command or manual check] |
| 2 | Application compiles and starts | [fill in the command or manual check] |
| 3 | Connectivity or schema validation passes | [fill in the command or manual check] |
| 4 | Changed surface responds correctly | [fill in the command or manual check] |
| 5 | No startup or migration regressions are visible | [fill in the command or manual check] |

---

## Database / ORM Consistency

Required for tasks that modify database structure, migrations, schema files, seed/bootstrap data, ORM models/entities, generated clients, repositories tied to ORM models, or persistence configuration.

| # | Check | How to validate |
|---|-------|----------------|
| 1 | Static DB/ORM consistency | [migration/schema validate, ORM generate/check, typecheck/compile, or manual mapping review command] |
| 2 | Local runtime environment | [command to start DB/services and app/worker locally; ask the human if not inferable] |
| 3 | Persistence smoke check | [minimal non-destructive check for migration/bootstrap and changed persistence path] |

---

## Notes For Automation

- If this file is missing, infer the smoke checks from the repository structure and build files before asking for human review.
- Prefer non-destructive commands and the smallest checks that still prove the real integration path works.
- If the project has migrations, schema generation, seed data, ORM models/entities, or generated DB clients, include static consistency checks and local runtime persistence smoke checks here.
- If the project has no backend or no external services, replace those rows with the closest reproducible local evidence.

---

## Human Review Gate

- Human developer review is required after smoke checks pass.
- If corrections are requested, rerun the impacted smoke checks before asking for approval again.
