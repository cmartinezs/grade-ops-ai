# Test Suite — task task-13

> Generated by `.planning/scripts/generate-test-suite.sh`.
> This file is deterministic scaffolding. Prefer repository commands over AI-generated test plans; use AI only to fill gaps the script cannot infer.

---

## Scope

- **Planning:** `001-assessment-creation`
- **Story:** story-02
- **Task:** task-13
- **Generated at:** 2026-07-21
- **Task context:** - **Affected files / components:** No new files expected. Possible small fixes if the sweep finds residue: any leftover fake-data code, unused imports, or a `features/assessment-creation/mocks/` directory if one was created and not cleaned up.

---

## Quality Gates

| Gate | Command or Evidence | Automation Level | Required When |
|------|---------------------|------------------|---------------|
| Unit tests | `npm test` | Automated when command exists | Any code behavior changes |
| Coverage | Add coverage command or define minimum coverage evidence. | Automated when command exists | New/changed logic, risk-sensitive branches |
| Integration tests | Add integration command that exercises real component boundaries; prefer Docker/Testcontainers/sandbox services. | Automated preferred | Components communicate through real adapters, DB, queues, HTTP, files, or generated clients |
| Acceptance / e2e tests | Add acceptance/e2e command; prefer Docker or an isolated environment. | Automated preferred | User-visible workflows, APIs, CLIs, deployment paths; for Maven services prefer Cucumber/Gherkin through profile `acceptanceTests`, booting the artifact in isolation and mocking external dependencies |
| Static analysis | `npm run lint` | Automated preferred | Every software task before PR review |
| Code style / formatting | `Run configured formatter/linter and inspect style violations` | Automated or checklist | Every changed source file |
| Architecture and design guides | Add automated architecture checks when available; otherwise review DDD, Hexagonal, DRY, SOLID, and GoF usage against project guides. | Automated if ArchUnit/equivalent exists; otherwise checklist | DDD, Hexagonal, DRY, SOLID, GoF, module boundaries, dependency direction |
| Runtime smoke | Use .planning/SMOKE-TESTS.md or define local runtime smoke checks. | Automated or documented manual evidence | App/service/worker startup, migrations, connectivity, changed surface |
| Security / dependency scan | Add dependency/security scan when available. | Automated when configured | Dependency, auth, input handling, secret, or deployment changes |
| Mutation / test strength | Optional: add mutation testing when configured. | Optional automated | Critical business rules, safety/security logic, complex branching |

---

## Independent Acceptance Environment

- Prefer Docker Compose, Testcontainers, local emulators, or a sandbox profile over shared developer services.
- For Maven services with Cucumber/Gherkin, prefer a dedicated `acceptanceTests` profile that packages/boots the artifact in an isolated environment and mocks external dependencies with local fakes, WireMock, MockServer, Testcontainers, or equivalent fixtures.
- Record exact environment variables, seed data, ports, and teardown commands.
- Tests must be non-destructive or use disposable data.

---

## Acceptance Dependency Inventory

| Dependency Type | Detected Signals | Acceptance Strategy |
|-----------------|------------------|---------------------|
| Internal modules/packages | None detected | Build from the same commit; do not call stale installed artifacts. |
| Runtime compose files | None detected | Use as fixture source or convert required services to Testcontainers/local fakes. |
| Environment/config files | None detected | Map every required variable to acceptance-safe values before booting the artifact. |
| Database/persistence | None detected | Prefer real disposable DB via Testcontainers; run migrations/bootstrap before scenarios. |
| External HTTP services | HTTP client or base-url references detected; route each external API to WireMock, MockServer, or local fake. | Mock with WireMock/MockServer/local fake; assert expected requests and responses. |
| Queues/brokers/events | Queue/broker references detected; use Testcontainers or local fake depending on protocol risk. | Use Testcontainers for protocol fidelity or local fake for contract-only behavior. |
| Object/file storage | Object/file storage references detected; use MinIO, temp directories, or local fake storage. | Use MinIO, temp directories, or fake storage; clean data after scenarios. |
| Secrets/credentials | Environment variable or secret-like names detected; acceptance profile must provide non-production dummy values. | Use dummy non-production values; never depend on real SaaS credentials. |

Acceptance setup must explicitly define build, boot, readiness, seed data, scenario execution, and teardown for every dependency above. If a dependency is intentionally out of scope, record the reason and the residual risk.

---

## Maven Acceptance Test Configuration

N/A — no Maven `pom.xml` detected.

---

## Coverage And Evidence

| Target | Minimum Evidence |
|--------|------------------|
| Planning | Each story has an executable or explicitly manual quality gate set |
| Story | Each task maps to at least unit/static/smoke evidence, plus integration or acceptance when component boundaries are crossed |
| Task | The task PR includes command output or linked CI evidence for every applicable gate |

---

## Execution Evidence Required

Full transcribed evidence lives in `../task-13-end-to-end-connection.md` § Verification Summary. Index below; do not duplicate — follow the link for exact output.

| Type | Source | Command | Parameters / profile / env | Environment | Config / scripts | Output log / report | Result |
|------|--------|---------|----------------------------|-------------|------------------|---------------------|--------|
| unit (web/) | detected | `npm run test -- --silent` | Jest, no special env | Local | `jest.config` | Task file Step 4 | 23 suites / 153 tests pass |
| unit (api/) | detected | `./mvnw test` | Full suite, includes Testcontainers-backed integration tests | Local, Docker via Testcontainers | `pom.xml`, `compose.smoke.yml` not used here (Testcontainers manages its own containers) | Task file Step 4 | 289 tests pass, BUILD SUCCESS |
| integration/regression | manual, red-green verified | `./mvnw test -Dtest=SecurityConfigTest` | New test asserting CORS `allowedMethods` contains `PATCH` | Local | `SecurityConfig.java`, `SecurityConfigTest.java` | Task file Step 2/3 bug table | Red (without fix) → 1 failure; Green (with fix) → pass |
| acceptance/e2e | manual, real browser | Playwright MCP walkthrough (registration → login → intake → draft render → edit/save/refresh → regenerate) | `NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_HOST=localhost:9099`, `api/compose.smoke.yml` (Postgres + Firebase Auth Emulator, project `demo-gradeops-smoke`) | Real local `web/` (`npm run dev`) + real local `api/` (`./mvnw spring-boot:run -Dspring-boot.run.profiles=smoke`) + Docker Compose | `api/compose.smoke.yml`, `api/src/main/resources/application-smoke.yml`, `web/src/lib/firebase/client.ts` (emulator-gated) | Task file § Verification Summary Step 2/3 | Pass — 3 real bugs found and fixed along the way (see bug table); one documented partial-coverage gap (version-history-after-regeneration, blocked by no `agents/` service in this environment) |
| smoke | manual | Full walkthrough itself (services ready, build/startup/connectivity) | Same as acceptance/e2e row | Same as above | Same as above | Task file § Verification Summary | Pass |
| static (web/) | detected | `npm run lint` | `next lint` / ESLint | Local | `eslint.config.mjs` | Task file Step 4 | No warnings or errors |
| architecture | detected | `./mvnw test` (includes `HexagonalArchitectureTest`, ArchUnit) | Runs as part of the full api/ suite | Local | ArchUnit rules in `HexagonalArchitectureTest` | Task file Step 4 | 6/6 pass |
| security | manual | Log/console review during the walkthrough for leaked secrets/tokens/draft content | N/A | Real browser + real api/ logs | N/A | Task file § Verification Summary, Environment cleanup | No sensitive payload content observed in captured console/server logs |

---

## Gaps To Fill Manually

- [x] Confirm commands are correct for this repository and adjust placeholders — done, see table above.
- [x] Add task-specific test names, coverage thresholds, fixtures, and data setup — `SecurityConfigTest.shouldAllowPatchForDraftEditSaves`; no coverage threshold defined for this repo beyond existing suites passing.
- [x] Complete the acceptance dependency inventory — real local Postgres + Firebase Auth Emulator via `api/compose.smoke.yml` (reused from task-12), real local `web/`+`api/` processes, no external HTTP/queue/object-storage dependencies exist in this scope; `agents/` (Gemini) is an intentionally out-of-scope dependency for this environment — the residual risk is that AI-generation's happy path itself was not exercised live, only its error-handling path; this is a pre-existing, documented environment ceiling from task-06/task-12, not a new gap introduced here.
- [x] Link code-style or architecture guide files — `docs/gradeops-ai-frontend-guidelines/02-ux-wireframes-y-maquetas.md` §2, `06-estado-datos-y-api.md` §13 (already listed in the task file's Generated Test Suite section).
- [x] Decide which optional gates are required — mutation testing not applicable (no complex branching introduced by this task's fixes); all other gates above are required and covered.
- [x] Record execution evidence metadata — see table above and the full transcripts in `../task-13-end-to-end-connection.md` § Verification Summary.
