# Update 2.x -> 3.10.0: Bring Older Planning Workspaces to the Current Baseline

## Goal

Bring any planning workspace that is still on the `2.x` line up to the `3.10.0` baseline.

This migration covers the full set of 3.x changes currently recorded in the changelog:

- v3.0.0 foundation changes
- v3.1.0 smoke-test planning support
- v3.2.0 training workspace layout fix
- v3.4.0 retrospective, edge-case, layered git, and update-version workflows
- v3.5.0 monorepo workspace boundaries, child-planning coordination, direct task branch setup, and DB/ORM validation safeguards
- v3.6.0 child-planning worktree isolation and worktree-prefixed branch naming
- post-merge local branch cleanup for layered task/story branches
- v3.7.0 optional PDR creation, deterministic test-suite generation, logging policy, and richer template examples
- v3.8.0 deterministic validation and documentation script delegation
- v3.9.0 deterministic release-management script delegation
- v3.10.0 deterministic reporting, atomization, story utility, task, lifecycle, initialization, update-version, and `/plan-story` helper delegation

## Scope

Inspect and update:

- `.planning/README.md`
- `.planning/GUIDE.md`
- `.planning/GLOSSARY.md`
- `.planning/TRACEABILITY-GLOBAL.md`
- `.planning/config.yml`
- `.planning/SMOKE-TESTS.md`
- `.planning/LOGGING.md`
- `.planning/scripts/doc-generate.mjs`
- `.planning/scripts/generate-test-suite.sh`
- `.planning/scripts/planning-mutate.mjs`
- `.planning/scripts/planning-archive.mjs`
- `.planning/scripts/planning-init.mjs`
- `.planning/scripts/planning-check.mjs`
- `.planning/scripts/planning-report.mjs`
- `.planning/scripts/planning-atomize.mjs`
- `.planning/scripts/planning-story.mjs`
- `.planning/scripts/planning-task.mjs`
- `.planning/scripts/release.mjs`
- `.planning/scripts/update-version.mjs`
- `.planning/_template/`
- `.planning/WORKFLOWS/`
- `.planning/TUTORIAL/`
- `.planning/active/`
- `.planning/finished/`
- any repo-level docs that mirror the planning command inventory or template structure

Do not edit:

- `.git/`
- dependency folders
- generated outputs
- source code or product documentation outside the planning system unless a link to a planning artifact must be corrected and the user explicitly confirms it

## Discovery

Run these searches from the project root:

```bash
rg -n "\b(plan-next|plan-doctor|plan-audit-docs|plan-smoke-config|plan-edge-case|plan-retrospective|plan-update-version|plan-decision|plan-test-suite|plan-atomize|doc-generate|planning-check|planning-report|planning-atomize|planning-story|planning-task|planning-archive|planning-done\.mjs|planning-init\.mjs|release\.mjs|update-version\.mjs|SMOKE-TESTS|LOGGING|TEST-SUITE|retrospective|STANDBY|story branch|task branch|git.base_branch|scope-NN|plan-scope|plan-scope-skip|doc-scope|pdr-)\b" .planning .releases README.md CHANGELOG.md docs .page planning-template skills
```

If `.releases/` does not exist, omit it from the search.

Classify matches before editing:

- **Planning-system addition**: new command, workflow, template, or doc that should exist in 3.10.0.
- **Versioned metadata**: version badges, package versions, changelog links, and command counts.
- **Legacy drift**: stale `scope` terminology or old commands that should be converted to `story`.

## Apply In Order

When updating a `2.x` workspace to `3.10.0`, apply the 3.x changes in this order:

1. v3.0.0 foundation
2. v3.1.0 smoke-test configuration
3. v3.2.0 layout and training polish
4. v3.4.0 retrospective and update-version workflows
5. v3.5.0 workspace-boundary, monorepo coordination, layered task git, and DB/ORM validation safeguards
6. v3.6.0 child-planning worktree isolation and branch-prefix safeguards
7. v3.7.0 PDR, logging, and test-suite safeguards
8. v3.8.0 deterministic script delegation
9. v3.9.0 deterministic release-management script delegation
10. v3.10.0 deterministic helper delegation for reporting, atomization, story utilities, task orchestration, lifecycle mutation, initialization, update-version, and `/plan-story`

If a later step already exists in the target workspace, leave it intact and only apply missing pieces.

### v3.0.0 foundation

Ensure the workspace has:

- `plan-next`, `plan-doctor`, and `plan-audit-docs` command references where applicable
- the project-guidance template folder under `planning-template/WORKFLOWS/06-PROJECT-GUIDANCE/`
- `planning-template/config.yml` defaults for `terminology`, `autonomy.level`, and `integrations.external_issues`
- risk and external issue fields in planning templates
- docs and tutorials that reference the current story-based model rather than old scope terminology

### v3.1.0 smoke-test configuration

Ensure the workspace has:

- `/plan-smoke-config`
- `.planning/SMOKE-TESTS.md`
- story and task instructions that reference the smoke-test plan before completion

### v3.2.0 layout polish

Ensure the workspace has:

- the updated training workspace layout guidance
- any related UI or tutorial copy that references the revised split-pane behavior

### v3.4.0 retrospective and update-version workflows

Ensure the workspace has:

- `/plan-edge-case`
- `/plan-retrospective`
- `RETROSPECTIVE-RAW.md`
- `RECORD-EDGE-CASE`
- `/plan-update-version`
- the `planning-template/update-version/` catalog
- the current command counts and version badges

### v3.5.0 workspace and execution safeguards

Ensure the workspace has:

- planning command guidance that uses only the current directory's `./.planning/` and never searches parent directories for `.planning/`
- monorepo parent/child coordination guidance: parent plannings coordinate, child artifact plannings own child implementation, and parent `01-expansion.md` records linked child plannings
- `01-expansion.md` template section `## Linked Child Plannings`
- `/plan-task` behavior that creates or reuses the story integration branch when invoked directly, before creating the task branch
- `/plan-story` behavior as optional orchestration for atomized stories, not a required pre-step and not a fallback atomizer
- post-merge local cleanup behavior: after each task PR is merged into the story branch, delete the local task branch with `git branch -d <task-branch>` from an updated story branch checkout
- post-merge local cleanup behavior: after the story PR is merged into `git.base_branch`, delete the local story branch with `git branch -d <story-branch>` from an updated base branch checkout
- guidance that remote branch deletion remains a PR/repository policy action, and merged branch cleanup must not use forced deletes
- task templates, smoke-test templates, and validation commands that require `Database / ORM Consistency Check` for DB/ORM changes
- `/plan-atomize` behavior that adds an explicit DB/ORM validation task after DB/ORM implementation tasks
- docs/tutorials that describe task PRs merging incrementally into the story branch before the final story PR targets `git.base_branch`

### v3.6.0 child planning worktree safeguards

Ensure the workspace has:

- guidance that git-enabled child plannings coordinated by a parent run in dedicated sibling worktrees
- examples that use generic placeholders only, such as `../<worktree-prefix>` and `<worktree-prefix>/story-NN-<slug>`
- `/plan-expand` behavior that creates or reuses child planning worktrees before creating linked child planning files
- `/plan-task`, `/plan-story`, and `/plan-done` behavior that preserves the worktree prefix before the story/task branch portion
- `CHECK-STORY-CONTEXT` behavior that recognizes prefixed child worktree branches and strips the prefix before locating `story-NN-*.md`

### v3.7.0 PDR, logging, and test-suite safeguards

Ensure the workspace has:

- `/plan-decision` guidance for optional Project Decision Records, with PDR creation only when a decision is explicit, accepted, cross-cutting, and supported by enough evidence
- `PDR-TEMPLATE.md` at the planning root and no default blank `pdr-*.md` artifact copied into every new planning
- concrete example values and rows in planning, expansion, story, task, traceability, retrospective, PDR, smoke-test, and idea-capture templates
- `/plan-test-suite`
- `.planning/scripts/generate-test-suite.sh`
- planning, story, and task `TEST-SUITE` artifacts or references to generated `test-suites/<task>-test-suite.md` files where applicable
- `/plan-task`, `/plan-atomize`, task templates, and validators that treat generated test-suite gates as first-class execution evidence
- `.planning/LOGGING.md`
- `/plan-task`, `/plan-atomize`, task templates, and validators that require intelligent logging evidence for software code tasks, including stack-specific recommendation and human decision when no logging mechanism is defined
- `/plan-task` git behavior that commits, pushes, opens or reuses the task PR, waits for external review, pushes requested corrections to the same PR, and marks the task `DONE` only after review approval

### v3.8.0 deterministic validation and documentation scripts

Ensure the workspace has:

- `.planning/scripts/planning-check.mjs`
- `.planning/scripts/doc-generate.mjs`
- `/plan-health`, `/plan-validate`, `/plan-task-validate`, `/us-status`, `/plan-audit-docs`, and `/plan-doctor` guidance that delegates deterministic read-only checks to `.planning/scripts/planning-check.mjs <subcommand>`
- `/doc-generate`, `/doc-task`, and `/doc-story` guidance that delegates deterministic documentation writing to the shared `.planning/scripts/doc-generate.mjs <planning-id> [story-id] [task-id]` entrypoint instead of per-skill scripts
- plugin verification guidance or local checks that validate deterministic scripts with `node --check` when Node is available

### v3.9.0 deterministic release-management script

Ensure the workspace has:

- `.planning/scripts/release.mjs`
- `/release-init`, `/release-new`, `/release-add`, `/release-remove`, and `/release-status` guidance that delegates deterministic release file updates and live planning status reads to the shared `.planning/scripts/release.mjs <subcommand>` entrypoint instead of per-skill scripts
- plugin verification guidance or local checks that validate `.planning/scripts/release.mjs` with `node --check` when Node is available

### v3.10.0 deterministic reporting script

Ensure the workspace has:

- `.planning/scripts/planning-report.mjs`
- `/plan-status`, `/plan-report`, `/plan-export`, `/plan-standup`, and `/plan-history` guidance that delegates deterministic inventory, metrics, exports, standup text, and status-transition history to the shared `.planning/scripts/planning-report.mjs <subcommand>` entrypoint instead of per-skill scripts
- plugin verification guidance or local checks that validate `.planning/scripts/planning-report.mjs` with `node --check` when Node is available

### v3.10.0 deterministic `/plan-atomize` script

Ensure the workspace has:

- `.planning/scripts/planning-atomize.mjs`
- `/plan-atomize` guidance that delegates repeatable worklist inspection, DONE/already-atomized skips, approved breakdown JSON validation, dependency ordering checks, task-file rendering, story `## Tasks` table rewrites, and test-suite command reporting to `.planning/scripts/planning-atomize.mjs`
- `/plan-atomize` guidance that keeps task decomposition, `[CHECK-ATOMICITY]`, DB/ORM validation-task decisions, traceability, and human approval in the skill
- a documented breakdown JSON interface for task title, workflow, dependencies, output, objective, technical design, implementation steps, verification, smoke checks, DB/ORM checks, logging, test suite details, and done criteria
- plugin verification guidance or local checks that validate `.planning/scripts/planning-atomize.mjs` with `node --check` when Node is available

### v3.10.0 deterministic `/plan-story` helpers

Ensure the workspace has:

- `.planning/scripts/planning-story.mjs`
- `/plan-story` guidance that delegates repeatable story inspection, atomization checks, next-task detection, story branch derivation, completed-task cleanup reporting, closeout reporting, and story status mutations to `.planning/scripts/planning-story.mjs execute-inspect|execute-start|execute-done|execute-finalize`
- `/plan-story` guidance that keeps context checks, task PR merge judgment, `/plan-task` invocation, final human review, smoke-test freshness judgment, and git/PR execution in the skill
- child-worktree guidance that passes `--worktree-prefix <worktree-prefix>` when the current branch does not already preserve the prefix before `story-NN-<slug>`
- plugin verification guidance or local checks that validate `.planning/scripts/planning-story.mjs` with `node --check` when Node is available

### v3.10.0 deterministic backlog/story utility script

Ensure the workspace has:

- `.planning/scripts/planning-story.mjs`
- `/us-new`, `/us-enrich`, `/us-split`, `/epic-enrich`, `/plan-enrich-story`, `/plan-enrich-epic`, and `/plan-split-story` guidance that delegates mechanical story operations to `.planning/scripts/planning-story.mjs <subcommand>` instead of per-skill scripts
- backlog subcommands for `backlog-inspect`, `backlog-new`, `backlog-enrich`, and `backlog-split`
- planning subcommands for `planning-inspect`, `planning-add-story`, `planning-enrich-story`, and `planning-split-story`
- clear guidance that backlog commands edit source backlog artifacts, while planning commands edit only the current working directory's `.planning/active/`
- plugin verification guidance or local checks that validate `.planning/scripts/planning-story.mjs` with `node --check` when Node is available

### v3.10.0 deterministic plan-task stage script

Ensure the workspace has:

- `.planning/scripts/planning-task.mjs`
- `/plan-task` guidance that delegates deterministic task stages to `.planning/scripts/planning-task.mjs <stage> <planning-id> <story-id> <task-id>`
- stage-first commands for `inspect`, `readiness`, `git-setup`, `start`, `publish`, `correction`, and `closeout`
- clear guidance that `git-setup`, `publish`, and `correction` only run git/gh commands with `--execute`, while `start` and `closeout` only mutate planning markdown with `--write`
- plugin verification guidance or local checks that validate `.planning/scripts/planning-task.mjs` with `node --check` when Node is available

### v3.10.0 deterministic update-version preflight script

Ensure the workspace has:

- `.planning/scripts/update-version.mjs`
- `/plan-update-version` guidance that delegates version parsing, adjacent major-pair chain selection, migration-file lookup/copying, `.planning/` boundary checks, git dirty preflight, and Markdown/JSON reporting to `.planning/scripts/update-version.mjs`
- plugin verification guidance or local checks that validate `.planning/scripts/update-version.mjs` with `node --check` when Node is available

### v3.10.0 deterministic plan-init script

Ensure the workspace has:

- `.planning/scripts/planning-init.mjs`
- `/plan-init` guidance that delegates template copying, non-destructive `--force` refreshes, area detection, AREA file generation, traceability table updates, config generation, and Markdown/JSON reporting to `.planning/scripts/planning-init.mjs`
- `/plan-init --force` behavior that preserves existing project-specific workspace files such as `config.yml`, `GUIDE.md`, `TRACEABILITY-GLOBAL.md`, `SMOKE-TESTS.md`, `LOGGING.md`, `PDR-TEMPLATE.md`, `PROMPTING.md`, `_template/`, `active/`, `finished/`, and `WORKFLOWS/05-SDLC-PHASE-GUIDANCE/` while still copying missing files and refreshing unowned system scripts/workflows
- plugin verification guidance or local checks that validate `.planning/scripts/planning-init.mjs` with `node --check` when Node is available

### v3.10.0 deterministic lifecycle mutation script

Ensure the workspace has:

- `.planning/scripts/planning-mutate.mjs`
- lifecycle mutation implementation files copied with the template scripts
- `/plan-done`, `/plan-clone`, `/plan-merge`, `/plan-rollback`, `/plan-retry`, `/plan-story-skip`, and `/plan-archive` guidance that delegates deterministic state/file changes to `.planning/scripts/planning-mutate.mjs <subcommand>`
- an `archive` lifecycle subcommand that audits closeout, moves `.planning/active/<planning-id>/` to `.planning/finished/<planning-id>/`, updates active/finished/root indexes, and requires dry-run review before writing
- plugin verification guidance or local checks that validate `.planning/scripts/planning-mutate.mjs` with `node --check` when Node is available

## Do Not Replace Blindly

Leave a match unchanged when it clearly refers to:

- OAuth scopes
- API permission scopes
- lexical scope or language/runtime scope
- CSS scoping
- database transaction scope
- business-domain scope that is not a planning execution unit

If a line is ambiguous, keep it unchanged and report it as residual follow-up.

## Verification

Run the residual search again:

```bash
rg -n "\b(plan-next|plan-doctor|plan-audit-docs|plan-smoke-config|plan-edge-case|plan-retrospective|plan-update-version|plan-decision|plan-test-suite|plan-atomize|doc-generate|planning-check|planning-report|planning-atomize|planning-story|planning-task|planning-archive|planning-mutate\.mjs|planning-init\.mjs|release\.mjs|update-version\.mjs|SMOKE-TESTS|LOGGING|TEST-SUITE|retrospective|STANDBY|story branch|task branch|git.base_branch|scope-NN|plan-scope|plan-scope-skip|doc-scope|pdr-)\b" .planning .releases README.md CHANGELOG.md docs .page planning-template skills
```

Then run:

```bash
/plan-validate
```

If the repo also tracks `.page/`, run its build after versioned content changes.

## Completion Report

Report:

- files updated to the 3.10.0 baseline
- files left unchanged because the 3.10.0 baseline already had them
- remaining legacy references and why they were kept
